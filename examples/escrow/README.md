# Escrow module e2e tests

Copy of escrow module e2e tests using published akash SDK.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python escrow.py
```

## Tests

▫️ Escrow module functionality tests

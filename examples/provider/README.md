# Provider module examples

Provider module functionality demonstration using published akash SDK.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python provider.py
```

## Features

▫️ Provider discovery and filtering
▫️ Provider capability queries
▫️ Provider registration
▫️ Provider updates
▫️ Region and attribute filtering

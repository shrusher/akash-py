# Authz module e2e tests

Copy of authz module e2e tests using published akash SDK.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python authz.py
```

## Tests

▫️ Authorization grants and revokes
▫️ Delegation permissions
▫️ Authorization queries
▫️ Grant validation
▫️ Permission management

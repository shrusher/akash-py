# Feegrant module e2e tests

Copy of feegrant module e2e tests using published akash SDK.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python feegrant.py
```

## Tests

▫️ Feegrant module functionality tests

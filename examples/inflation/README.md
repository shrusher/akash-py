# Inflation module e2e tests

Copy of inflation module e2e tests using published akash SDK.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python inflation.py
```

## Tests

▫️ Inflation module functionality tests
